[project]
name = "opcsim"
version = "0.1.0"
description = "OttimaSystem: servidor OPC-UA de simulação para testes do opc-worker (dev-only)"
requires-python = ">=3.12,<3.13"
dependencies = [
  "asyncua>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/opcsim"]
