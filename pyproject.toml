[project]
name = "ottima-workspace"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
# pyfuzzylite 8.x declara numpy<2.0 (poetry "^1.20"), mas o próprio repo mantém a
# regra NPY201 ativa (compatível com numpy 2) e o workspace exige numpy>=2.5 (ADR-029):
# o override substitui o pin declarativo atrasado da lib.
override-dependencies = ["numpy>=2.5"]

[tool.uv.workspace]
members = ["packages/*", "services/*", "tests/opcsim"]

[dependency-groups]
dev = [
  "pytest>=8.3",
  "pytest-asyncio>=0.24",
  "testcontainers>=4.8",
  "httpx>=0.27",
  # Cliente WebSocket da L2 (E2E-F3-09, RF-305). Já vinha no ambiente por `uvicorn[standard]`,
  # mas a suíte o importa direto: transitiva usada de propósito é dependência declarada.
  "websockets>=13",
  "ruff>=0.6",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["packages", "services", "tests"]
markers = [
  "e2e: testes end-to-end contra o stack docker compose (fora do run default)",
  "slow: testes de carga/desempenho (RNF-02) — fora do run default, rodados via -m slow",
  "rnf09: suíte MPC↔TFS do aceite RNF-09 (ADR-022)",
]
# --import-mode=importlib: os serviços têm tests/test_health.py homônimos sem __init__.py
addopts = "-m 'not e2e and not slow' --import-mode=importlib"

[tool.ruff]
line-length = 100
target-version = "py312"
# docs/ guarda documentos normativos e planos (artefatos históricos), não código-fonte:
# o ruff não deve reformatar os blocos Python embutidos neles.
extend-exclude = ["docs/"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]

[tool.ruff.lint.flake8-bugbear]
# Depends/Security no default do parâmetro é o idioma do FastAPI (B008 não se aplica)
extend-immutable-calls = ["fastapi.Depends", "fastapi.Security"]

[tool.ruff.lint.isort]
known-first-party = ["ottima_core", "ottima_api", "ottima_opc_worker", "ottima_flow_runtime", "ottima_recorder", "opcsim", "testkit"]
